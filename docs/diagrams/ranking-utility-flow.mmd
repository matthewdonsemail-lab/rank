flowchart TD
  LISTS["Ranked candidate lists"] --> NORMALIZE["normalizeScores / mergeRankings"]
  NORMALIZE --> OUTPUT["Ranked result utility"]
  OUTPUT --> NOTE["Utility only; not a current machine stage"]
