# Both are needed for the box on the landing page to start a real run.
# Without them the page still shows and says runs are not switched on.
VITE_CONVEX_URL=https://your-deployment.convex.cloud
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
